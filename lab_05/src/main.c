#include <stdio.h>

#include "../inc/time_func.h"
#include "../inc/arr_queue.h"
#include "../inc/errors.h"

int main(void)
{
    int rc = OK;

    arr_queue arr; float tmp;

    arr_queue_init(&arr);


    return rc;
}