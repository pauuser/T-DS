#include <stdio.h>

#include "../inc/time_func.h"
#include "../inc/arr_queue.h"
#include "../inc/errors.h"
#include "../inc/list_queue.h"

int main(void)
{
    setbuf(stdout, NULL);

    int rc = OK;
    
    return rc;
}